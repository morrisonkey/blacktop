<%= render "title" %>

<% page_to_render = {
  1 => "name",
  2 => "photo",
  3 => "tags",
  4 => "items",
  5 => "profile"
}
%>

<%= form_tag do %>
  <%= render page_to_render[@page] %>
  <%= hidden_field_tag :page, @page %>
  <%= render "footer", :page => @page %>
<% end %>